# BLE
CONFIG_ZMK_BLE=y
CONFIG_BT_DEVICE_NAME="sabai-right"

# USB (for flashing and wired fallback)
CONFIG_USB_DEVICE_STACK=y
CONFIG_ZMK_USB=y

# Logging off for performance
CONFIG_ZMK_LOG_LEVEL_DBG=n
